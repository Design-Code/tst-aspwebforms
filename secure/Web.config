<?xml version="1.0"?>
<configuration>
    <system.web>
        <authorization>
            <!-- deny users who are not logged in from getting to secure folder-->
            <deny users="?"/>
            
        </authorization>
    </system.web>
</configuration>
